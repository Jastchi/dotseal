# Generated by setup.sh -- never commit key material, not even test fixtures
# (secret scanners flag it forever, and it normalizes a bad habit).
.dotseal.key
.env.enc

# The test .env contains only fake values and is intentionally committed.
!.env
