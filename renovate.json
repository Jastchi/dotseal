{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "enabledManagers": ["pep621", "github-actions"],
  "osvVulnerabilityAlerts": true,
  "vulnerabilityAlerts": {
    "enabled": true,
    "automerge": true,
    "minimumReleaseAge": "0 days",
    "prCreation": "immediate",
    "internalChecksFilter": "none"
  },
  "pep621": {
    "rangeStrategy": "pin"
  },
  "github-actions": {
    "pinDigests": true
  },
  "lockFileMaintenance": {
    "enabled": true,
    "automerge": true,
    "schedule": ["every 2 weeks on monday"]
  },
  "automergeType": "pr",
  "automergeStrategy": "squash",
  "platformAutomerge": true,
  "minimumReleaseAge": "7 days",
  "internalChecksFilter": "strict",
  "prCreation": "not-pending",
  "packageRules": [
    {
      "description": "Never update requires-python; Python version is managed manually.",
      "matchDepTypes": ["requires-python"],
      "enabled": false
    },
    {
      "description": "Don't gate one-time pin/pinDigest operations on minimumReleaseAge. The value being pinned is what's currently in use, so the 7-day window adds no safety here. Without this, grouped pin PRs can get stuck pending forever because at least one dependency in the group typically has a release within the last 7 days.",
      "matchUpdateTypes": ["pin", "pinDigest"],
      "minimumReleaseAge": "0 days"
    },
    {
      "description": "Group all non-major Python (PEP 621) updates into one PR and automerge. Excludes ruff and ty which are managed separately. rangeStrategy=pin ensures uv installs the exact version Renovate chose, preventing resolver drift that would bypass minimumReleaseAge.",
      "matchManagers": ["pep621"],
      "matchUpdateTypes": ["minor", "patch", "digest"],
      "matchPackageNames": ["!ruff", "!ty"],
      "groupName": "Python dependencies",
      "automerge": true
    },
    {
      "description": "Group all Python (PEP 621) major updates into one PR — no automerge.",
      "matchManagers": ["pep621"],
      "matchUpdateTypes": ["major"],
      "groupName": "Python major updates"
    },
    {
      "description": "Group all non-major GitHub Actions updates into one PR and automerge. Bi-weekly schedule prevents volatile actions from continuously rebasing the PR and resetting the 7-day stability timer.",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["minor", "patch", "digest"],
      "groupName": "GitHub Actions dependencies",
      "schedule": ["every 2 weeks on monday"],
      "automerge": true
    },
    {
      "description": "Group all GitHub Actions major updates into one PR — no automerge. Bi-weekly schedule for consistency with the non-major rule.",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["major"],
      "groupName": "GitHub Actions major updates",
      "schedule": ["every 2 weeks on monday"]
    },
    {
      "description": "Keep the pytest<3.10 entry on 8.x. pytest 9+ requires Python>=3.10, so the marker entry for older Pythons must stay below 9. matchCurrentVersion targets only the 8.x pin; the 9.x pin is unaffected.",
      "matchPackageNames": ["pytest"],
      "matchCurrentVersion": ">=8.0.0 <9.0.0",
      "allowedVersions": ">=8.0.0 <9.0.0"
    },
    {
      "description": "ruff and ty: allow pre-release versioning so updates are never suppressed. The global 7-day minimumReleaseAge still applies; vulnerabilityAlerts overrides it to immediate for security fixes.",
      "matchPackageNames": ["ruff", "ty"],
      "ignoreUnstable": false,
      "automerge": true,
      "groupName": "dev tools"
    }
  ]
}
